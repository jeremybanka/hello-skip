import {} from "@skipruntime/api";
export class ResourceBuilder {
    constructor(builder) {
        this.builder = builder;
    }
    build(parameters) {
        const builder = this.builder;
        return new builder(parameters);
    }
}
//# sourceMappingURL=binding.js.map